<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body>
    <%- include('../partials/header') %>
    <main>
        <div class="container">
            <% groups.forEach(group => { %>
                <%- include('group', { group }) %>
            <% }) %> 
        </div>
    </main>
</body>
</html>